class Poi extends GameObject {

  constructor({
    image,
    width,
    height,
    posX,
    posY,
    posDX,
    posDY,
    canvas
              }){
    super({
      image,
      width,
      height,
      posX,
      posY,
      posDX,
      posDY,
      canvas
      }
    );
  }
}